name: Deploy

# Build image rồi deploy MCP server lên Cloud Run.
# Xác thực bằng Workload Identity Federation — không dùng service account key JSON.

on:
  push:
    branches: [develop]
    paths:
      - "backend/**"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:
    inputs:
      environment:
        description: Môi trường deploy
        type: choice
        options: [dev, prod]
        default: dev

permissions:
  contents: read
  id-token: write

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

env:
  ENVIRONMENT: ${{ inputs.environment || 'dev' }}
  APP_NAME: nexlab-recruit

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment || 'dev' }}
    steps:
      - uses: actions/checkout@v4

      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_DEPLOY_SERVICE_ACCOUNT }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Đăng nhập Artifact Registry
        run: gcloud auth configure-docker ${{ secrets.GCP_REGION }}-docker.pkg.dev --quiet

      - name: Build & push
        env:
          REGISTRY: ${{ secrets.GCP_REGION }}-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/${{ env.APP_NAME }}-${{ env.ENVIRONMENT }}
        run: |
          IMAGE="$REGISTRY/scan-cv:${{ github.sha }}"
          docker build -t "$IMAGE" -t "$REGISTRY/scan-cv:latest" backend
          docker push "$IMAGE"
          docker push "$REGISTRY/scan-cv:latest"
          echo "IMAGE=$IMAGE" >> "$GITHUB_ENV"

      # Service và Job dùng CHUNG image nhưng là hai tài nguyên riêng — quên
      # cập nhật Job thì nó chạy mãi image cũ mà không ai nhận ra.
      - name: Deploy Cloud Run service
        run: |
          gcloud run deploy "${{ env.APP_NAME }}-${{ env.ENVIRONMENT }}-web" \
            --image "$IMAGE" \
            --region "${{ secrets.GCP_REGION }}" \
            --project "${{ secrets.GCP_PROJECT_ID }}" \
            --quiet

      - name: Cập nhật Cloud Run job
        run: |
          gcloud run jobs update "${{ env.APP_NAME }}-${{ env.ENVIRONMENT }}-scan-job" \
            --image "$IMAGE" \
            --region "${{ secrets.GCP_REGION }}" \
            --project "${{ secrets.GCP_PROJECT_ID }}" \
            --quiet

      - name: Kiểm tra sau khi deploy
        run: |
          URL=$(gcloud run services describe "${{ env.APP_NAME }}-${{ env.ENVIRONMENT }}-web" \
            --region "${{ secrets.GCP_REGION }}" \
            --project "${{ secrets.GCP_PROJECT_ID }}" \
            --format 'value(status.url)')
          echo "Trang quản trị: $URL/admin"
          curl -fsS "$URL/health" && echo

          ON_JOB=$(gcloud run jobs describe "${{ env.APP_NAME }}-${{ env.ENVIRONMENT }}-scan-job" \
            --region "${{ secrets.GCP_REGION }}" \
            --project "${{ secrets.GCP_PROJECT_ID }}" \
            --format 'value(spec.template.spec.template.spec.containers[0].image)')
          echo "Image trên job: $ON_JOB"
          if [ "$ON_JOB" != "$IMAGE" ]; then
            echo "Job vẫn dùng image cũ — deploy chưa trọn vẹn." >&2
            exit 1
          fi
